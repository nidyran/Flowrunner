spring:
  application:
    name: flowrunner

flowrunner:
  flow:
    dimensions:
      - key: application
        name: Application
      - key: environment
        name: Environment
      - key: channel
        name: Channel
